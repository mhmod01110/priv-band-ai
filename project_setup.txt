# Legal Policy Analyzer API - دليل الإعداد الكامل

## 1. هيكل المشروع

```
legal-policy-analyzer/
├── .env
├── .gitignore
├── README.md
├── requirements.txt
├── setup.py
├── pyproject.toml
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── models.py
│   ├── config.py
│   ├── prompts/
│   │   ├── __init__.py
│   │   ├── policy_matcher.py
│   │   ├── compliance_analyzer.py
│   │   └── compliance_rules.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── openai_service.py
│   │   └── analyzer_service.py
│   └── utils/
│       ├── __init__.py
│       └── validators.py
├── static/
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js
├── templates/
│   └── index.html
└── tests/
    ├── __init__.py
    └── test_api.py
```

## 2. خطوات الإعداد

### الخطوة 1: إنشاء البيئة الافتراضية

```bash
# إنشاء مجلد المشروع
mkdir legal-policy-analyzer
cd legal-policy-analyzer

# إنشاء البيئة الافتراضية
python -m venv venv

# تفعيل البيئة
# في Windows:
venv\Scripts\activate
# في Linux/Mac:
source venv/bin/activate
```

### الخطوة 2: تثبيت المكتبات

## 3. الملفات الأساسية

### requirements.txt
```
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
openai==1.3.0
python-multipart==0.0.6
jinja2==3.1.2
aiofiles==23.2.1
pytest==7.4.3
httpx==0.25.1
```

### .env (قم بإنشاء هذا الملف وأضف مفتاح OpenAI الخاص بك)
```
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview
```

### .gitignore
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# Testing
.pytest_cache/
htmlcov/
.coverage

# OS
.DS_Store
Thumbs.db

# Logs - مجلد Logs بالكامل
logs/
*.log

# Archives
*.zip
*.tar.gz
```

## 4. خطوات التشغيل النهائية

### تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### تشغيل التطبيق
```bash
# طريقة 1: باستخدام uvicorn مباشرة
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# طريقة 2: باستخدام Python
python -m uvicorn app.main:app --reload
```

### الوصول للتطبيق
افتح المتصفح على: `http://localhost:8000`

### تشغيل الاختبارات
```bash
pytest tests/ -v
```

## 5. استخدام API

### مثال باستخدام cURL:
```bash
curl -X POST "http://localhost:8000/api/analyze" \
  -H "Content-Type: application/json" \
  -d '{
    "shop_name": "متجر الأزياء",
    "shop_specialization": "ملابس نسائية",
    "policy_type": "سياسات الاسترجاع و الاستبدال",
    "policy_text": "يحق للعميل إرجاع المنتج خلال 7 أيام..."
  }'
```

### مثال باستخدام Python:
```python
import requests

url = "http://localhost:8000/api/analyze"
data = {
    "shop_name": "متجر الأزياء",
    "shop_specialization": "ملابس نسائية",
    "policy_type": "سياسات الاسترجاع و الاستبدال",
    "policy_text": "يحق للعميل إرجاع المنتج خلال 7 أيام من تاريخ الاستلام..."
}

response = requests.post(url, json=data)
print(response.json())
```

## 6. التغليف والتوزيع

### بناء الحزمة:
```bash
python setup.py sdist bdist_wheel
```

### التثبيت المحلي:
```bash
pip install -e .
```

### النشر على PyPI:
```bash
pip install twine
twine upload dist/*
```

## 7. الملاحظات المهمة

1. **مفتاح OpenAI**: تأكد من إضافة مفتاح API صحيح في ملف `.env`
2. **التكلفة**: استخدام GPT-4 له تكلفة، راقب استهلاكك
3. **الأمان**: لا تشارك ملف `.env` أو تحمله على Git
4. **الأداء**: التحليل يستغرق 10-30 ثانية حسب طول النص
5. **اللغة**: النظام مصمم للعمل مع النصوص العربية فقط

## 8. استكشاف الأخطاء

### مشكلة: خطأ في الاتصال بـ OpenAI
**الحل**: تحقق من مفتاح API في ملف `.env`

### مشكلة: ModuleNotFoundError
**الحل**: تأكد من تثبيت جميع المكتبات: `pip install -r requirements.txt`

### مشكلة: بطء في الاستجابة
**الحل**: هذا طبيعي، GPT-4 يحتاج وقت للتحليل

## 9. التطوير المستقبلي

- [ ] إضافة المزيد من أنواع السياسات
- [ ] تحسين دقة التحليل
- [ ] إضافة قاعدة بيانات للتقارير
- [ ] إنشاء لوحة تحكم للإحصائيات
- [ ] دعم تحليل PDF مباشرة
- [ ] API للمصادقة والأمان

---

**تم بنجاح! 🎉**

المشروع جاهز للاستخدام. استمتع بتحليل السياسات القانونية!
